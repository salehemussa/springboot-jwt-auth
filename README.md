# springboot-jwt-auth
A secure RESTful API built with Spring Boot 3, Spring Security, and JWT (JSON Web Tokens) for authentication and authorization. It includes user registration, login, and role-based access control — all backed by MariaDB.
